module.exports = function reverse(n) {

    // const n = -150;
    // const data = Number(Math.abs(n).toString().split('').reverse().join(''));
    // console.log(data);
    return Number(Math.abs(n).toString().split('').reverse().join(''));

}
